ho creato i componenti principali dell'app il login(src/components/Login.vue)
la registrazione(src/components/Register.vue.vue) e la home(src/components/Register.vue.vue)

sono presenti delle funzioni con lo stesso nome in Login.vue e Register.vue quelle funzioni controlleranno solamente
il verificatosi della registrazione o del login

Il file index.js nella directory:"router" permette di cambiare le pagine (prestare molta attenzione alle modifice)
Il file main.js crea l'app anche qui (prestare molta attenzione alle modifiche)

il file index.html e style.css importano librerie e definiscono lo stile generale dell'app (il css ha molti commenti)

Implementata la comunicazione con il back-end vedere il file App.vue nel percorso: src/App.vue

Implementata la possibilità di caricare file audio nel server ed ogni volta che viene fatto l'accesso
vengono ricaricati il codice dell'impmenetazione si può vedere il src/Home.vue

Il file "Setting.vue" è ancora in sviluppo in ogni caso lì verrà data la possibilità di modificare la foto profilo, visualizzare dati vari ecc...